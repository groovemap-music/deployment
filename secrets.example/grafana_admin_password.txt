# Generate with: openssl rand -base64 24
# Grafana admin login for the production overlay, where anonymous access is
# disabled — this is the only credential that reaches the dashboards.
your-grafana-admin-password-here
